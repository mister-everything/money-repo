import { describe, expect, it } from "vitest";

describe("todoAgent", () => {
  it("should pass basic test", () => {
    expect(true).toBe(true);
  });

  it("should work with TODO", () => {
    expect("TODO").toBe("TODO");
  });
});
